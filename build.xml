[00:23, 05/05/2025] Xuan Liang: <project name="MyWebApp" default="deploy" basedir=".">
    <target name="init">
        <mkdir dir="build/classes"/>
    </target>

    <target name="compile" depends="init">
        <javac srcdir="src" destdir="build/classes"/>
    </target>

    <target name="package" depends="compile">
        <war destfile="build/MyWebApp.war" webxml="web/WEB-INF/web.xml">
            <fileset dir="web"/>
            <classes dir="build/classes"/>
        </war>
    </target>

    <target name="deploy" depends="package">
        <copy file="build/MyWebApp.war" todir="/opt/tomcat/webapps"/>
    </target>
</project>
[00:32, 05/05/2025] Xuan Liang: <?xml version="1.0" encoding="UTF-8"?>
<project name="IntergrateGUI" default="deploy" basedir=".">

    <!-- Define directories -->
    <property name="src.dir" value="src"/>
    <property name="web.dir" value="web"/>
    <property name="build.dir" value="build"/>
    <property name="classes.dir" value="${build.dir}/classes"/>
    <property name="war.file" value="${build.dir}/IntergrateGUI.war"/>
    <property name="tomcat.webapps" value="/opt/tomcat/apache-tomcat-9.0.104/webapps"/>

    <!-- Init build dirs -->
    <target name="init">
        <mkdir dir="${classes.dir}"/>
    </target>

    <!-- Compile Java sources -->
    <target name="compile" depends="init">
        <javac srcdir="${src.dir}" destdir="${classes.dir}" includeantruntime="false"/>
    </target>

    <!-- Package WAR -->
    <target name="war" depends="compile">
        <war destfile="${war.file}" webxml="${web.dir}/WEB-INF/web.xml">
            <fileset dir="${web.dir}"/>
            <classes dir="${classes.dir}"/>
        </war>
    </target>

    <!-- Deploy WAR to Tomcat -->
    <target name="deploy" depends="war">
        <copy file="${war.file}" todir="${tomcat.webapps}"/>
    </target>

</project>
